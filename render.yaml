services:
  - type: web
    name: eventsec-backend
    runtime: python
    rootDir: backend
    plan: starter
    buildCommand: pip install -r requirements.txt
    preDeployCommand: bash scripts/render_predeploy.sh
    startCommand: bash scripts/render_start.sh
    healthCheckPath: /healthz
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: eventsec-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: OPENSEARCH_REQUIRED
        value: "false"
      - key: UI_BASE_URL
        value: "https://eventsec-ihae.vercel.app"
      - key: CORS_ORIGINS
        value: "https://eventsec-ihae.vercel.app"
      - key: CORS_ALLOW_ORIGIN_REGEX
        value: "https://.*\\.vercel\\.app"
      - key: COOKIE_SECURE
        value: "true"
      - key: COOKIE_SAMESITE
        value: "lax"

  - type: worker
    name: eventsec-vuln-worker
    runtime: python
    rootDir: backend
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: bash scripts/render_worker.sh
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: eventsec-db
          property: connectionString
      - key: VULN_INTEL_WORKER_ROLE
        value: "worker"
      - key: OPENSEARCH_REQUIRED
        value: "false"

databases:
  - name: eventsec-db
    # Render no longer allows legacy "starter" for new databases.
    # Replace with a supported plan in the Render UI (e.g., free/basic).
    plan: REPLACE_ME
    databaseName: eventsec
    user: eventsec
