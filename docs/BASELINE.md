# Baseline (local env)

## Docker / Compose
- Docker: **not available** (`docker` command missing in this environment).
- Docker Compose: **not available** (blocked by missing Docker binary).

## Service status
Not verified in this environment because Docker is unavailable.

## Health endpoints
Not verified in this environment because the stack could not be started.

## OpenSearch events-v1
Not verified in this environment because OpenSearch could not be started.
